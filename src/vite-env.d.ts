/// <reference types="vite/client" />

// Compile-time constant injected by Vite `define`. Sourced from package.json
// in vite.config.ts so bumping the package.json version is the single source
// of truth for the header version display.
declare const __APP_VERSION__: string;
